server:
  port: 80

spring:
  application:
    name: cloud-order-service

eureka:
  client:
    # 不注册自己
    register-with-eureka: true
    # 表示自己是注册中心，不用检索服务
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7001/eureka